<h1>Editing asset_type</h1>

<% form_for(@asset_type) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @asset_type %> |
<%= link_to 'Back', asset_types_path %>